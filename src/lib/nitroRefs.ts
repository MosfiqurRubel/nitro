import { createRef } from "react";

export const heroCanRef = createRef<HTMLDivElement>();
export const productionCanRef = createRef<HTMLDivElement>();
export const flavorCanRef = createRef<HTMLDivElement>();